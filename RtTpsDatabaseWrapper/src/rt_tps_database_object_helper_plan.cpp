//////////////////////////////////////////////////////////////////////////
/// \defgroup Radio Therapy Business Unit
///  Copyright, (c) Shanghai United Imaging Healthcare Inc., 2016
///  All rights reserved.
///
///  \author  ZHOU qiangqiang  mailto:qiangqiang.zhou@united-imaging.com
///
///  \file      rt_tps_database_object_helper_plan.cpp
///  \brief     This file was generated by CodeGenerater.exe 
///             From database version: 1.1.11
///
///  \version 1.0
///  \date    8/8/2017
///  \{
//////////////////////////////////////////////////////////////////////////

#include "StdAfx.h"
#include "rt_tps_database_object_helper.h"
#include "RtTpsDatabaseWrapper/rt_tps_database_interface_object_plan.h"
#include "boost/date_time/posix_time/time_parsers.hpp"
typedef boost::posix_time::ptime DATETIME_BOOST;
typedef boost::posix_time::time_duration TIME_BOOST;
typedef boost::gregorian::date DATE_BOOST;

RT_TPS_DATABASE_BEGIN_NAMESPACE;

bool RtDatabaseHelper::InsertUpdateDBFromObject(const RtPlan& src, bool bInsert)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }
    //25 parameters
    unsigned int field_num = 0;
    std::string sSql("");
    std::string sName("");
    if (!bInsert)
    {
        for (unsigned int i(0); i < PLAN_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_PLAN_FIELD[i];
            if (sName.empty())
            {
                sName += std::string(file_name) + "=?";
            }
            else
            {
                sName += "," + std::string(file_name) + "=?";
            }
            ++field_num;
        }
        sSql = "UPDATE tmsplan SET " + sName + " WHERE uid='" + src.get_uid() + "'";
    }
    else
    {
        std::string sValues("");
        for (unsigned int i(0); i < PLAN_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_PLAN_FIELD[i];
            if (sName.empty())
            {
                sName += std::string(file_name);
                sValues += "?";
            }
            else
            {
                sName += "," + std::string(file_name);
                sValues += ",?";
            }
            ++field_num;
        }
        sSql = "INSERT INTO tmsplan (" + sName + ") VALUES (" + sValues + ")";
    }
    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)sSql.size()))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_prepare(), failed %s\n", mysql_stmt_error(stmt));
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[field_num];
    memset(bind, 0, sizeof(MYSQL_BIND)*field_num);
    my_bool     isnull = true;
    std::string	data_uid = "";
    std::string	data_courseuid = "";
    std::string	data_imageseriesuid = "";
    std::string	data_setuppoiuid = "";
    std::string	data_name = "";
    std::string	data_sopinstanceuid = "";
    int			data_plansourcetype = 0;
    float		data_bodythickness = 0.f;
    std::string	data_planner = "";
    float		data_totalgoal = 0.f;
    int			data_tpsverno = 0;
    int			data_tmsverno = 0;
    std::string	data_planid = "";
    int			data_origintype = 0;
    int			data_displaymode = 0;
    int			data_hundredpercentdefinition = 0;
    double		data_specifieddose = 0.;
    int			data_rendermode = 0;
    float		data_transparency = 0.f;
    bool		data_enableskindoseview = false;
    float		data_maxskindose = 0.f;
    float		data_transparency3d = 0.f;
    std::string	data_patientposition = "";
    std::string	data_description = "";
    MYSQL_TIME	data_updatetime;

    unsigned int iCount = 0;
    for (unsigned int i(0); i < PLAN_FIELD_MAX; ++i)
    {
        if (!src.is_dirty(i)) continue;
        if(!src.has_field(i))
        {
            bind[iCount].is_null = &isnull;
            ++iCount;
            continue;
        }
        switch (i)
        {
        case 0: 	//uid varchar
            {
                data_uid = src.get_uid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_uid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_uid.size();
            }
            break;
        case 1: 	//courseuid varchar
            {
                data_courseuid = src.get_courseuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_courseuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_courseuid.size();
            }
            break;
        case 2: 	//imageseriesuid varchar
            {
                data_imageseriesuid = src.get_imageseriesuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_imageseriesuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_imageseriesuid.size();
            }
            break;
        case 3: 	//setuppoiuid varchar
            {
                data_setuppoiuid = src.get_setuppoiuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_setuppoiuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_setuppoiuid.size();
            }
            break;
        case 4: 	//name varchar
            {
                data_name = src.get_name();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_name.c_str();
                bind[iCount].buffer_length = (unsigned long)data_name.size();
            }
            break;
        case 5: 	//sopinstanceuid varchar
            {
                data_sopinstanceuid = src.get_sopinstanceuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_sopinstanceuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_sopinstanceuid.size();
            }
            break;
        case 6: 	//plansourcetype int
            {
                data_plansourcetype = src.get_plansourcetype();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_plansourcetype;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 7: 	//bodythickness float
            {
                data_bodythickness = src.get_bodythickness();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_bodythickness;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 8: 	//planner varchar
            {
                data_planner = src.get_planner();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_planner.c_str();
                bind[iCount].buffer_length = (unsigned long)data_planner.size();
            }
            break;
        case 9: 	//totalgoal float
            {
                data_totalgoal = src.get_totalgoal();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_totalgoal;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 10: 	//tpsverno int
            {
                data_tpsverno = src.get_tpsverno();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_tpsverno;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 11: 	//tmsverno int
            {
                data_tmsverno = src.get_tmsverno();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_tmsverno;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 12: 	//planid varchar
            {
                data_planid = src.get_planid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_planid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_planid.size();
            }
            break;
        case 13: 	//origintype int
            {
                data_origintype = src.get_origintype();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_origintype;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 14: 	//displaymode int
            {
                data_displaymode = src.get_displaymode();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_displaymode;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 15: 	//hundredpercentdefinition int
            {
                data_hundredpercentdefinition = src.get_hundredpercentdefinition();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_hundredpercentdefinition;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 16: 	//specifieddose double
            {
                data_specifieddose = src.get_specifieddose();
                bind[iCount].buffer_type = MYSQL_TYPE_DOUBLE;
                bind[iCount].buffer = (void*)&data_specifieddose;
                bind[iCount].buffer_length = sizeof(double);
            }
            break;
        case 17: 	//rendermode int
            {
                data_rendermode = src.get_rendermode();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_rendermode;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 18: 	//transparency float
            {
                data_transparency = src.get_transparency();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_transparency;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 19: 	//enableskindoseview tinyint
            {
                data_enableskindoseview = src.get_enableskindoseview();
                bind[iCount].buffer_type = MYSQL_TYPE_TINY;
                bind[iCount].buffer = (void*)&data_enableskindoseview;
                bind[iCount].buffer_length = sizeof(bool);
            }
            break;
        case 20: 	//maxskindose float
            {
                data_maxskindose = src.get_maxskindose();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_maxskindose;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 21: 	//transparency3d float
            {
                data_transparency3d = src.get_transparency3d();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_transparency3d;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 22: 	//patientposition varchar
            {
                data_patientposition = src.get_patientposition();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_patientposition.c_str();
                bind[iCount].buffer_length = (unsigned long)data_patientposition.size();
            }
            break;
        case 23: 	//description varchar
            {
                data_description = src.get_description();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_description.c_str();
                bind[iCount].buffer_length = (unsigned long)data_description.size();
            }
            break;
        case 24: 	//updatetime timestamp
            {
                ConvertDateTime2DB(src.get_updatetime(), &data_updatetime);
                bind[iCount].buffer_type = MYSQL_TYPE_TIMESTAMP;
                bind[iCount].buffer = (void*)&data_updatetime;
                if (src.get_updatetime().is_not_a_date_time()) bind[iCount].is_null = &isnull;
            }
            break;
        default:
            break;
        }

        ++iCount;
    }
    if (mysql_stmt_bind_param(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_param(), failed "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_execute(), failed %s\n", mysql_stmt_error(stmt));
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
	DEL_ARRAY(bind);
	RtDatabaseObject* rtDatabaseObject = const_cast<RtPlan*>(&src);
	rtDatabaseObject->setdirty_all_fields(false);
    return true;
}

bool RtDatabaseHelper::FetchDataFromDB(const std::string& sSql, std::vector<RtPlan*>& vList)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }

    MYSQL_RES  *prepare_meta_result = nullptr;
    //tmsplan    25 parameters
    const int field_num = PLAN_FIELD_MAX;
    unsigned long MAX_LENGTH[field_num] = {0};
    MAX_LENGTH[0] = 64;
    MAX_LENGTH[1] = 64;
    MAX_LENGTH[2] = 64;
    MAX_LENGTH[3] = 64;
    MAX_LENGTH[4] = 256;
    MAX_LENGTH[5] = 64;
    MAX_LENGTH[6] = 0;
    MAX_LENGTH[7] = 0;
    MAX_LENGTH[8] = 256;
    MAX_LENGTH[9] = 0;
    MAX_LENGTH[10] = 0;
    MAX_LENGTH[11] = 0;
    MAX_LENGTH[12] = 256;
    MAX_LENGTH[13] = 0;
    MAX_LENGTH[14] = 0;
    MAX_LENGTH[15] = 0;
    MAX_LENGTH[16] = 0;
    MAX_LENGTH[17] = 0;
    MAX_LENGTH[18] = 0;
    MAX_LENGTH[19] = 0;
    MAX_LENGTH[20] = 0;
    MAX_LENGTH[21] = 0;
    MAX_LENGTH[22] = 16;
    MAX_LENGTH[23] = 1024;
    MAX_LENGTH[24] = 0;

    //std::string	data_uid
    //std::string	data_courseuid
    //std::string	data_imageseriesuid
    //std::string	data_setuppoiuid
    //std::string	data_name
    //std::string	data_sopinstanceuid
    int	data_plansourcetype = 0;
    float	data_bodythickness = 0.f;
    //std::string	data_planner
    float	data_totalgoal = 0.f;
    int	data_tpsverno = 0;
    int	data_tmsverno = 0;
    //std::string	data_planid
    int	data_origintype = 0;
    int	data_displaymode = 0;
    int	data_hundredpercentdefinition = 0;
    double	data_specifieddose = 0.;
    int	data_rendermode = 0;
    float	data_transparency = 0.f;
    bool	data_enableskindoseview = false;
    float	data_maxskindose = 0.f;
    float	data_transparency3d = 0.f;
    //std::string	data_patientposition
    //std::string	data_description
    MYSQL_TIME	data_updatetime;

    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)strlen(sSql.c_str())))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), SELECT failed."<<mysql_stmt_error(stmt);
        return false;
    }

    //Fetch result set meta information
    prepare_meta_result = mysql_stmt_result_metadata(stmt);
    if (!prepare_meta_result) 
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_result_metadata(), returned no meta information "<<mysql_stmt_error(stmt);
        return false;
    }

    // Get total columns in the query and validate column count
    const unsigned int column_count= mysql_num_fields(prepare_meta_result);
    if (column_count != field_num)
    {
        TPS_LOG_DEV_ERROR<<"invalid column count returned by MySQL";
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[column_count];
    memset(bind, 0, sizeof(MYSQL_BIND)*column_count);
    unsigned long* length = new unsigned long[column_count];
    memset(length, 0, sizeof(unsigned long) * column_count);
    my_bool* is_null = new my_bool[column_count];
    memset(is_null, 0, sizeof(my_bool) * column_count);
    //Execute the SELECT query
    char* data_string[field_num];
    for (int i=0; i<field_num; ++i)
    {
        const unsigned long ulSize = MAX_LENGTH[i] + 1;// + 1 for string's \0 
        data_string[i] = new char[ulSize];
        memset(data_string[i], 0, ulSize*sizeof(char));
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed"<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    for (unsigned int i(0); i<column_count; ++i)
    {
        bind[i].is_null= &is_null[i];
        bind[i].length= &length[i];
        switch (i)
        {
        case 6: 	//plansourcetype int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_plansourcetype;
            break;
        case 7: 	//bodythickness float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_bodythickness;
            break;
        case 9: 	//totalgoal float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_totalgoal;
            break;
        case 10: 	//tpsverno int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_tpsverno;
            break;
        case 11: 	//tmsverno int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_tmsverno;
            break;
        case 13: 	//origintype int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_origintype;
            break;
        case 14: 	//displaymode int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_displaymode;
            break;
        case 15: 	//hundredpercentdefinition int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_hundredpercentdefinition;
            break;
        case 16: 	//specifieddose double
            bind[i].buffer_type= MYSQL_TYPE_DOUBLE;
            bind[i].buffer= (char *)&data_specifieddose;
            break;
        case 17: 	//rendermode int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_rendermode;
            break;
        case 18: 	//transparency float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_transparency;
            break;
        case 19: 	//enableskindoseview tinyint
            bind[i].buffer_type= MYSQL_TYPE_TINY;
            bind[i].buffer= (char *)&data_enableskindoseview;
            break;
        case 20: 	//maxskindose float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_maxskindose;
            break;
        case 21: 	//transparency3d float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_transparency3d;
            break;
        case 24: 	//updatetime timestamp
            bind[i].buffer_type= MYSQL_TYPE_TIMESTAMP;
            bind[i].buffer= (char *)&data_updatetime;
            break;
        default:
            bind[i].buffer_type= MYSQL_TYPE_STRING;
            bind[i].buffer= (char *)data_string[i];
            bind[i].buffer_length= MAX_LENGTH[i];
            break;
        }
    }

    // Bind the result buffers
    if (mysql_stmt_bind_result(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    //Now buffer all results to client
    if (mysql_stmt_store_result(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_store_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    // Fetch all rows 
    int ret(1);
    RtPlan* pDis = nullptr;
    while(true)
    {
        ret = mysql_stmt_fetch(stmt);
        if (ret!=0 && ret!=MYSQL_DATA_TRUNCATED) break;
        pDis = new RtPlan();
        for (unsigned int i(0); i<column_count; ++i)
        {
            if (is_null[i]) continue;
            switch (i)
            {
            case 0: 	//uid varchar
                pDis->set_uid((char *)bind[i].buffer);
                break;
            case 1: 	//courseuid varchar
                pDis->set_courseuid((char *)bind[i].buffer);
                break;
            case 2: 	//imageseriesuid varchar
                pDis->set_imageseriesuid((char *)bind[i].buffer);
                break;
            case 3: 	//setuppoiuid varchar
                pDis->set_setuppoiuid((char *)bind[i].buffer);
                break;
            case 4: 	//name varchar
                pDis->set_name((char *)bind[i].buffer);
                break;
            case 5: 	//sopinstanceuid varchar
                pDis->set_sopinstanceuid((char *)bind[i].buffer);
                break;
            case 6: 	//plansourcetype int
                pDis->set_plansourcetype(data_plansourcetype);
                break;
            case 7: 	//bodythickness float
                pDis->set_bodythickness(data_bodythickness);
                break;
            case 8: 	//planner varchar
                pDis->set_planner((char *)bind[i].buffer);
                break;
            case 9: 	//totalgoal float
                pDis->set_totalgoal(data_totalgoal);
                break;
            case 10: 	//tpsverno int
                pDis->set_tpsverno(data_tpsverno);
                break;
            case 11: 	//tmsverno int
                pDis->set_tmsverno(data_tmsverno);
                break;
            case 12: 	//planid varchar
                pDis->set_planid((char *)bind[i].buffer);
                break;
            case 13: 	//origintype int
                pDis->set_origintype(data_origintype);
                break;
            case 14: 	//displaymode int
                pDis->set_displaymode(data_displaymode);
                break;
            case 15: 	//hundredpercentdefinition int
                pDis->set_hundredpercentdefinition(data_hundredpercentdefinition);
                break;
            case 16: 	//specifieddose double
                pDis->set_specifieddose(data_specifieddose);
                break;
            case 17: 	//rendermode int
                pDis->set_rendermode(data_rendermode);
                break;
            case 18: 	//transparency float
                pDis->set_transparency(data_transparency);
                break;
            case 19: 	//enableskindoseview tinyint
                pDis->set_enableskindoseview(data_enableskindoseview);
                break;
            case 20: 	//maxskindose float
                pDis->set_maxskindose(data_maxskindose);
                break;
            case 21: 	//transparency3d float
                pDis->set_transparency3d(data_transparency3d);
                break;
            case 22: 	//patientposition varchar
                pDis->set_patientposition((char *)bind[i].buffer);
                break;
            case 23: 	//description varchar
                pDis->set_description((char *)bind[i].buffer);
                break;
            case 24: 	//updatetime timestamp
                {
                    DATETIME_BOOST updatetime;
                    ConvertDateTime2Boost(data_updatetime, &updatetime);
                    pDis->set_updatetime(updatetime);
                }
                break;
            default:
                break;
            }
        }
        pDis->setdirty_all_fields(false);
        vList.push_back(pDis);
    }
    for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
    DEL_ARRAY(bind);
    DEL_ARRAY(length);
    DEL_ARRAY(is_null);

    // Free the prepared result metadata
    mysql_free_result(prepare_meta_result);

    // Close the statement
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement " << mysql_stmt_error(stmt);
        return false;
    }

    return true;
}

RT_TPS_DATABASE_END_NAMESPACE