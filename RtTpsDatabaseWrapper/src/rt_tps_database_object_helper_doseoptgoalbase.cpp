//////////////////////////////////////////////////////////////////////////
/// \defgroup Radio Therapy Business Unit
///  Copyright, (c) Shanghai United Imaging Healthcare Inc., 2016
///  All rights reserved.
///
///  \author  ZHOU qiangqiang  mailto:qiangqiang.zhou@united-imaging.com
///
///  \file      rt_tps_database_object_helper_doseoptgoalbase.cpp
///  \brief     This file was generated by CodeGenerater.exe 
///             From database version: 1.1.11
///
///  \version 1.0
///  \date    8/8/2017
///  \{
//////////////////////////////////////////////////////////////////////////

#include "StdAfx.h"
#include "rt_tps_database_object_helper.h"
#include "RtTpsDatabaseWrapper/rt_tps_database_interface_object_doseoptgoalbase.h"
#include "boost/date_time/posix_time/time_parsers.hpp"
typedef boost::posix_time::ptime DATETIME_BOOST;
typedef boost::posix_time::time_duration TIME_BOOST;
typedef boost::gregorian::date DATE_BOOST;

RT_TPS_DATABASE_BEGIN_NAMESPACE;

bool RtDatabaseHelper::InsertUpdateDBFromObject(const RtDoseoptgoalbase& src, bool bInsert)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }
    //18 parameters
    unsigned int field_num = 0;
    std::string sSql("");
    std::string sName("");
    if (!bInsert)
    {
        for (unsigned int i(0); i < DOSEOPTGOALBASE_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_DOSEOPTGOALBASE_FIELD[i];
			//if("ID" == std::string(file_name)) 
			//{
			//	continue;
			//}
            if (sName.empty())
            {
                sName += std::string(file_name) + "=?";
            }
            else
            {
                sName += "," + std::string(file_name) + "=?";
            }
            ++field_num;
        }
        sSql = "UPDATE tmsdoseoptgoalbase SET " + sName + " WHERE uid='" + src.get_uid() + "'";
    }
    else
    {
        std::string sValues("");
        for (unsigned int i(0); i < DOSEOPTGOALBASE_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_DOSEOPTGOALBASE_FIELD[i];
            if (sName.empty())
            {
                sName += std::string(file_name);
                sValues += "?";
            }
            else
            {
                sName += "," + std::string(file_name);
                sValues += ",?";
            }
            ++field_num;
        }
        sSql = "INSERT INTO tmsdoseoptgoalbase (" + sName + ") VALUES (" + sValues + ")";
    }
    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)sSql.size()))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_prepare(), failed %s\n", mysql_stmt_error(stmt));
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[field_num];
    memset(bind, 0, sizeof(MYSQL_BIND)*field_num);
    my_bool     isnull = true;
    std::string	data_uid = "";
    int			data_id = 0;
    std::string	data_normgroupuid = "";
    std::string	data_voiuid = "";
    int			data_doseoptgoaltype = 0;
    int			data_ishardlimit = 0;
    float		data_voiweight = 0.f;
    bool		data_isjoined = false;
    float		data_eud = 0.f;
    float		data_a = 0.f;
    float		data_maxdose = 0.f;
    float		data_mindose = 0.f;
    float		data_dose = 0.f;
    float		data_dvhdose = 0.f;
    float		data_volume = 0.f;
    bool		data_islowerbound = false;
    float		data_eudresult = 0.f;
    MYSQL_TIME	data_updatetime;

    unsigned int iCount = 0;
    for (unsigned int i(0); i < DOSEOPTGOALBASE_FIELD_MAX; ++i)
    {
        if (!src.is_dirty(i)) continue;
        if(!src.has_field(i))
        {
            bind[iCount].is_null = &isnull;
            ++iCount;
            continue;
        }
        switch (i)
        {
        case 0: 	//uid varchar
            {
                data_uid = src.get_uid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_uid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_uid.size();
            }
            break;
        case 1: 	//id int
            {
                data_id = src.get_id();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_id;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 2: 	//normgroupuid varchar
            {
                data_normgroupuid = src.get_normgroupuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_normgroupuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_normgroupuid.size();
            }
            break;
        case 3: 	//voiuid varchar
            {
                data_voiuid = src.get_voiuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_voiuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_voiuid.size();
            }
            break;
        case 4: 	//doseoptgoaltype int
            {
                data_doseoptgoaltype = src.get_doseoptgoaltype();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_doseoptgoaltype;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 5: 	//ishardlimit int
            {
                data_ishardlimit = src.get_ishardlimit();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_ishardlimit;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 6: 	//voiweight float
            {
                data_voiweight = src.get_voiweight();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_voiweight;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 7: 	//isjoined tinyint
            {
                data_isjoined = src.get_isjoined();
                bind[iCount].buffer_type = MYSQL_TYPE_TINY;
                bind[iCount].buffer = (void*)&data_isjoined;
                bind[iCount].buffer_length = sizeof(bool);
            }
            break;
        case 8: 	//eud float
            {
                data_eud = src.get_eud();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_eud;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 9: 	//a float
            {
                data_a = src.get_a();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_a;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 10: 	//maxdose float
            {
                data_maxdose = src.get_maxdose();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_maxdose;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 11: 	//mindose float
            {
                data_mindose = src.get_mindose();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mindose;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 12: 	//dose float
            {
                data_dose = src.get_dose();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_dose;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 13: 	//dvhdose float
            {
                data_dvhdose = src.get_dvhdose();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_dvhdose;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 14: 	//volume float
            {
                data_volume = src.get_volume();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_volume;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 15: 	//islowerbound tinyint
            {
                data_islowerbound = src.get_islowerbound();
                bind[iCount].buffer_type = MYSQL_TYPE_TINY;
                bind[iCount].buffer = (void*)&data_islowerbound;
                bind[iCount].buffer_length = sizeof(bool);
            }
            break;
        case 16: 	//eudresult float
            {
                data_eudresult = src.get_eudresult();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_eudresult;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 17: 	//updatetime timestamp
            {
                ConvertDateTime2DB(src.get_updatetime(), &data_updatetime);
                bind[iCount].buffer_type = MYSQL_TYPE_TIMESTAMP;
                bind[iCount].buffer = (void*)&data_updatetime;
                if (src.get_updatetime().is_not_a_date_time()) bind[iCount].is_null = &isnull;
            }
            break;
        default:
            break;
        }

        ++iCount;
    }
    if (mysql_stmt_bind_param(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_param(), failed "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_execute(), failed %s\n", mysql_stmt_error(stmt));
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
	DEL_ARRAY(bind);
	RtDatabaseObject* rtDatabaseObject = const_cast<RtDoseoptgoalbase*>(&src);
	rtDatabaseObject->setdirty_all_fields(false);
    return true;
}

bool RtDatabaseHelper::FetchDataFromDB(const std::string& sSql, std::vector<RtDoseoptgoalbase*>& vList)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }

    MYSQL_RES  *prepare_meta_result = nullptr;
    //tmsdoseoptgoalbase    18 parameters
    const int field_num = DOSEOPTGOALBASE_FIELD_MAX;
    unsigned long MAX_LENGTH[field_num] = {0};
    MAX_LENGTH[0] = 64;
    MAX_LENGTH[1] = 0;
    MAX_LENGTH[2] = 64;
    MAX_LENGTH[3] = 64;
    MAX_LENGTH[4] = 0;
    MAX_LENGTH[5] = 0;
    MAX_LENGTH[6] = 0;
    MAX_LENGTH[7] = 0;
    MAX_LENGTH[8] = 0;
    MAX_LENGTH[9] = 0;
    MAX_LENGTH[10] = 0;
    MAX_LENGTH[11] = 0;
    MAX_LENGTH[12] = 0;
    MAX_LENGTH[13] = 0;
    MAX_LENGTH[14] = 0;
    MAX_LENGTH[15] = 0;
    MAX_LENGTH[16] = 0;
    MAX_LENGTH[17] = 0;

    //std::string	data_uid
    int	data_id = 0;
    //std::string	data_normgroupuid
    //std::string	data_voiuid
    int	data_doseoptgoaltype = 0;
    int	data_ishardlimit = 0;
    float	data_voiweight = 0.f;
    bool	data_isjoined = false;
    float	data_eud = 0.f;
    float	data_a = 0.f;
    float	data_maxdose = 0.f;
    float	data_mindose = 0.f;
    float	data_dose = 0.f;
    float	data_dvhdose = 0.f;
    float	data_volume = 0.f;
    bool	data_islowerbound = false;
    float	data_eudresult = 0.f;
    MYSQL_TIME	data_updatetime;

    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)strlen(sSql.c_str())))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), SELECT failed."<<mysql_stmt_error(stmt);
        return false;
    }

    //Fetch result set meta information
    prepare_meta_result = mysql_stmt_result_metadata(stmt);
    if (!prepare_meta_result) 
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_result_metadata(), returned no meta information "<<mysql_stmt_error(stmt);
        return false;
    }

    // Get total columns in the query and validate column count
    const unsigned int column_count= mysql_num_fields(prepare_meta_result);
    if (column_count != field_num)
    {
        TPS_LOG_DEV_ERROR<<"invalid column count returned by MySQL";
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[column_count];
    memset(bind, 0, sizeof(MYSQL_BIND)*column_count);
    unsigned long* length = new unsigned long[column_count];
    memset(length, 0, sizeof(unsigned long) * column_count);
    my_bool* is_null = new my_bool[column_count];
    memset(is_null, 0, sizeof(my_bool) * column_count);
    //Execute the SELECT query
    char* data_string[field_num];
    for (int i=0; i<field_num; ++i)
    {
        const unsigned long ulSize = MAX_LENGTH[i] + 1;// + 1 for string's \0 
        data_string[i] = new char[ulSize];
        memset(data_string[i], 0, ulSize*sizeof(char));
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed"<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    for (unsigned int i(0); i<column_count; ++i)
    {
        bind[i].is_null= &is_null[i];
        bind[i].length= &length[i];
        switch (i)
        {
        case 1: 	//id int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_id;
            break;
        case 4: 	//doseoptgoaltype int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_doseoptgoaltype;
            break;
        case 5: 	//ishardlimit int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_ishardlimit;
            break;
        case 6: 	//voiweight float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_voiweight;
            break;
        case 7: 	//isjoined tinyint
            bind[i].buffer_type= MYSQL_TYPE_TINY;
            bind[i].buffer= (char *)&data_isjoined;
            break;
        case 8: 	//eud float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_eud;
            break;
        case 9: 	//a float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_a;
            break;
        case 10: 	//maxdose float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_maxdose;
            break;
        case 11: 	//mindose float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mindose;
            break;
        case 12: 	//dose float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_dose;
            break;
        case 13: 	//dvhdose float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_dvhdose;
            break;
        case 14: 	//volume float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_volume;
            break;
        case 15: 	//islowerbound tinyint
            bind[i].buffer_type= MYSQL_TYPE_TINY;
            bind[i].buffer= (char *)&data_islowerbound;
            break;
        case 16: 	//eudresult float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_eudresult;
            break;
        case 17: 	//updatetime timestamp
            bind[i].buffer_type= MYSQL_TYPE_TIMESTAMP;
            bind[i].buffer= (char *)&data_updatetime;
            break;
        default:
            bind[i].buffer_type= MYSQL_TYPE_STRING;
            bind[i].buffer= (char *)data_string[i];
            bind[i].buffer_length= MAX_LENGTH[i];
            break;
        }
    }

    // Bind the result buffers
    if (mysql_stmt_bind_result(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    //Now buffer all results to client
    if (mysql_stmt_store_result(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_store_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    // Fetch all rows 
    int ret(1);
    RtDoseoptgoalbase* pDis = nullptr;
    while(true)
    {
        ret = mysql_stmt_fetch(stmt);
        if (ret!=0 && ret!=MYSQL_DATA_TRUNCATED) break;
        pDis = new RtDoseoptgoalbase();
        for (unsigned int i(0); i<column_count; ++i)
        {
            if (is_null[i]) continue;
            switch (i)
            {
            case 0: 	//uid varchar
                pDis->set_uid((char *)bind[i].buffer);
                break;
            case 1: 	//id int
                pDis->set_id(data_id);
                break;
            case 2: 	//normgroupuid varchar
                pDis->set_normgroupuid((char *)bind[i].buffer);
                break;
            case 3: 	//voiuid varchar
                pDis->set_voiuid((char *)bind[i].buffer);
                break;
            case 4: 	//doseoptgoaltype int
                pDis->set_doseoptgoaltype(data_doseoptgoaltype);
                break;
            case 5: 	//ishardlimit int
                pDis->set_ishardlimit(data_ishardlimit);
                break;
            case 6: 	//voiweight float
                pDis->set_voiweight(data_voiweight);
                break;
            case 7: 	//isjoined tinyint
                pDis->set_isjoined(data_isjoined);
                break;
            case 8: 	//eud float
                pDis->set_eud(data_eud);
                break;
            case 9: 	//a float
                pDis->set_a(data_a);
                break;
            case 10: 	//maxdose float
                pDis->set_maxdose(data_maxdose);
                break;
            case 11: 	//mindose float
                pDis->set_mindose(data_mindose);
                break;
            case 12: 	//dose float
                pDis->set_dose(data_dose);
                break;
            case 13: 	//dvhdose float
                pDis->set_dvhdose(data_dvhdose);
                break;
            case 14: 	//volume float
                pDis->set_volume(data_volume);
                break;
            case 15: 	//islowerbound tinyint
                pDis->set_islowerbound(data_islowerbound);
                break;
            case 16: 	//eudresult float
                pDis->set_eudresult(data_eudresult);
                break;
            case 17: 	//updatetime timestamp
                {
                    DATETIME_BOOST updatetime;
                    ConvertDateTime2Boost(data_updatetime, &updatetime);
                    pDis->set_updatetime(updatetime);
                }
                break;
            default:
                break;
            }
        }
        pDis->setdirty_all_fields(false);
        vList.push_back(pDis);
    }
    for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
    DEL_ARRAY(bind);
    DEL_ARRAY(length);
    DEL_ARRAY(is_null);

    // Free the prepared result metadata
    mysql_free_result(prepare_meta_result);

    // Close the statement
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement " << mysql_stmt_error(stmt);
        return false;
    }

    return true;
}

RT_TPS_DATABASE_END_NAMESPACE