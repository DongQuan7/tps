//////////////////////////////////////////////////////////////////////////
/// \defgroup Radio Therapy Business Unit
///  Copyright, (c) Shanghai United Imaging Healthcare Inc., 2016
///  All rights reserved.
///
///  \author  ZHOU qiangqiang  mailto:qiangqiang.zhou@united-imaging.com
///
///  \file      rt_tps_database_object_helper_accessorychunk.cpp
///  \brief     This file was generated by CodeGenerater.exe 
///             From database version: 1.1.11
///
///  \version 1.0
///  \date    8/8/2017
///  \{
//////////////////////////////////////////////////////////////////////////

#include "StdAfx.h"
#include "rt_tps_database_object_helper.h"
#include "RtTpsDatabaseWrapper/rt_tps_database_interface_object_accessorychunk.h"
#include "boost/date_time/posix_time/time_parsers.hpp"
typedef boost::posix_time::ptime DATETIME_BOOST;
typedef boost::posix_time::time_duration TIME_BOOST;
typedef boost::gregorian::date DATE_BOOST;

RT_TPS_DATABASE_BEGIN_NAMESPACE;

bool RtDatabaseHelper::InsertUpdateDBFromObject(const RtAccessorychunk& src, bool bInsert)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }
    //21 parameters
    unsigned int field_num = 0;
    std::string sSql("");
    std::string sName("");
    if (!bInsert)
    {
        for (unsigned int i(0); i < ACCESSORYCHUNK_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_ACCESSORYCHUNK_FIELD[i];
            if (sName.empty())
            {
                sName += std::string(file_name) + "=?";
            }
            else
            {
                sName += "," + std::string(file_name) + "=?";
            }
            ++field_num;
        }
        sSql = "UPDATE tmsaccessorychunk SET " + sName + " WHERE uid='" + src.get_uid() + "'";
    }
    else
    {
        std::string sValues("");
        for (unsigned int i(0); i < ACCESSORYCHUNK_FIELD_MAX; ++i)
        {
            if (!src.is_dirty(i)) continue;
            const char* file_name = ENUM2STRING_ACCESSORYCHUNK_FIELD[i];
            if (sName.empty())
            {
                sName += std::string(file_name);
                sValues += "?";
            }
            else
            {
                sName += "," + std::string(file_name);
                sValues += ",?";
            }
            ++field_num;
        }
        sSql = "INSERT INTO tmsaccessorychunk (" + sName + ") VALUES (" + sValues + ")";
    }
    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)sSql.size()))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_prepare(), failed %s\n", mysql_stmt_error(stmt));
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[field_num];
    memset(bind, 0, sizeof(MYSQL_BIND)*field_num);
    my_bool     isnull = true;
    std::string	data_uid = "";
    std::string	data_accessoryuid = "";
    std::string	data_commissionedunituid = "";
    float		data_mu = 0.f;
    float		data_mu_dx = 0.f;
    float		data_mu_dr = 0.f;
    float		data_mu_da = 0.f;
    float		data_mu_dv = 0.f;
    float		data_hvl_slope = 0.f;
    float		data_density = 0.f;
    int			data_datatype = 0;
    std::string	data_mcphoparam1 = "";
    std::string	data_mcphoparam2 = "";
    std::string	data_mcphoparam3 = "";
    std::string	data_mcphoparam4 = "";
    std::string	data_mcphoparam5 = "";
    std::string	data_mcphoparam6 = "";
    std::string	data_mcphooffaxisvec = "";
    int			data_algorithmtype = 0;
    std::string	data_crc = "";
    MYSQL_TIME	data_updatetime;

    unsigned int iCount = 0;
    for (unsigned int i(0); i < ACCESSORYCHUNK_FIELD_MAX; ++i)
    {
        if (!src.is_dirty(i)) continue;
        if(!src.has_field(i))
        {
            bind[iCount].is_null = &isnull;
            ++iCount;
            continue;
        }
        switch (i)
        {
        case 0: 	//uid varchar
            {
                data_uid = src.get_uid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_uid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_uid.size();
            }
            break;
        case 1: 	//accessoryuid varchar
            {
                data_accessoryuid = src.get_accessoryuid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_accessoryuid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_accessoryuid.size();
            }
            break;
        case 2: 	//commissionedunituid varchar
            {
                data_commissionedunituid = src.get_commissionedunituid();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_commissionedunituid.c_str();
                bind[iCount].buffer_length = (unsigned long)data_commissionedunituid.size();
            }
            break;
        case 3: 	//mu float
            {
                data_mu = src.get_mu();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mu;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 4: 	//mu_dx float
            {
                data_mu_dx = src.get_mu_dx();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mu_dx;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 5: 	//mu_dr float
            {
                data_mu_dr = src.get_mu_dr();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mu_dr;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 6: 	//mu_da float
            {
                data_mu_da = src.get_mu_da();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mu_da;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 7: 	//mu_dv float
            {
                data_mu_dv = src.get_mu_dv();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_mu_dv;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 8: 	//hvl_slope float
            {
                data_hvl_slope = src.get_hvl_slope();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_hvl_slope;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 9: 	//density float
            {
                data_density = src.get_density();
                bind[iCount].buffer_type = MYSQL_TYPE_FLOAT;
                bind[iCount].buffer = (void*)&data_density;
                bind[iCount].buffer_length = sizeof(float);
            }
            break;
        case 10: 	//datatype int
            {
                data_datatype = src.get_datatype();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_datatype;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 11: 	//mcphoparam1 varchar
            {
                data_mcphoparam1 = src.get_mcphoparam1();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam1.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam1.size();
            }
            break;
        case 12: 	//mcphoparam2 varchar
            {
                data_mcphoparam2 = src.get_mcphoparam2();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam2.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam2.size();
            }
            break;
        case 13: 	//mcphoparam3 varchar
            {
                data_mcphoparam3 = src.get_mcphoparam3();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam3.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam3.size();
            }
            break;
        case 14: 	//mcphoparam4 varchar
            {
                data_mcphoparam4 = src.get_mcphoparam4();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam4.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam4.size();
            }
            break;
        case 15: 	//mcphoparam5 varchar
            {
                data_mcphoparam5 = src.get_mcphoparam5();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam5.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam5.size();
            }
            break;
        case 16: 	//mcphoparam6 varchar
            {
                data_mcphoparam6 = src.get_mcphoparam6();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_mcphoparam6.c_str();
                bind[iCount].buffer_length = (unsigned long)data_mcphoparam6.size();
            }
            break;
        case 17: 	//mcphooffaxisvec blob
            {
                data_mcphooffaxisvec = src.get_mcphooffaxisvec();
                if(data_mcphooffaxisvec.empty()) bind[iCount].is_null = &isnull;
                bind[iCount].buffer_type= MYSQL_TYPE_BLOB;
                bind[iCount].buffer= (void *)data_mcphooffaxisvec.c_str();
                bind[iCount].buffer_length= (unsigned long)data_mcphooffaxisvec.size();
            }
            break;
        case 18: 	//algorithmtype int
            {
                data_algorithmtype = src.get_algorithmtype();
                bind[iCount].buffer_type = MYSQL_TYPE_LONG;
                bind[iCount].buffer = (void*)&data_algorithmtype;
                bind[iCount].buffer_length = sizeof(int);
            }
            break;
        case 19: 	//crc varchar
            {
                data_crc = src.get_crc();
                bind[iCount].buffer_type = MYSQL_TYPE_VAR_STRING;
                bind[iCount].buffer = (void*)data_crc.c_str();
                bind[iCount].buffer_length = (unsigned long)data_crc.size();
            }
            break;
        case 20: 	//updatetime timestamp
            {
                ConvertDateTime2DB(src.get_updatetime(), &data_updatetime);
                bind[iCount].buffer_type = MYSQL_TYPE_TIMESTAMP;
                bind[iCount].buffer = (void*)&data_updatetime;
                if (src.get_updatetime().is_not_a_date_time()) bind[iCount].is_null = &isnull;
            }
            break;
        default:
            break;
        }

        ++iCount;
    }
    if (mysql_stmt_bind_param(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_param(), failed "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed "<< mysql_stmt_error(stmt);
        TPS_PRINTF_DEBUG("mysql_stmt_execute(), failed %s\n", mysql_stmt_error(stmt));
        DEL_ARRAY(bind);
        return false;
    }
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement "<< mysql_stmt_error(stmt);
        DEL_ARRAY(bind);
        return false;
    }
    DEL_ARRAY(bind);
	RtDatabaseObject* rtDatabaseObject = const_cast<RtAccessorychunk*>(&src);
	rtDatabaseObject->setdirty_all_fields(false);
    return true;
}

bool RtDatabaseHelper::FetchDataFromDB(const std::string& sSql, std::vector<RtAccessorychunk*>& vList)
{
    MYSQL_STMT* stmt = mysql_stmt_init(m_pMySql);
    if (!stmt)
    {
        TPS_LOG_DEV_ERROR<<"mysql_stmt_init(), out of memory";
        return false;
    }

    MYSQL_RES  *prepare_meta_result = nullptr;
    //tmsaccessorychunk    21 parameters
    const int field_num = ACCESSORYCHUNK_FIELD_MAX;
    unsigned long MAX_LENGTH[field_num] = {0};
    MAX_LENGTH[0] = 64;
    MAX_LENGTH[1] = 64;
    MAX_LENGTH[2] = 64;
    MAX_LENGTH[3] = 0;
    MAX_LENGTH[4] = 0;
    MAX_LENGTH[5] = 0;
    MAX_LENGTH[6] = 0;
    MAX_LENGTH[7] = 0;
    MAX_LENGTH[8] = 0;
    MAX_LENGTH[9] = 0;
    MAX_LENGTH[10] = 0;
    MAX_LENGTH[11] = 256;
    MAX_LENGTH[12] = 256;
    MAX_LENGTH[13] = 256;
    MAX_LENGTH[14] = 256;
    MAX_LENGTH[15] = 256;
    MAX_LENGTH[16] = 256;
    MAX_LENGTH[17] = 65535;
    MAX_LENGTH[18] = 0;
    MAX_LENGTH[19] = 64;
    MAX_LENGTH[20] = 0;

    //std::string	data_uid
    //std::string	data_accessoryuid
    //std::string	data_commissionedunituid
    float	data_mu = 0.f;
    float	data_mu_dx = 0.f;
    float	data_mu_dr = 0.f;
    float	data_mu_da = 0.f;
    float	data_mu_dv = 0.f;
    float	data_hvl_slope = 0.f;
    float	data_density = 0.f;
    int	data_datatype = 0;
    //std::string	data_mcphoparam1
    //std::string	data_mcphoparam2
    //std::string	data_mcphoparam3
    //std::string	data_mcphoparam4
    //std::string	data_mcphoparam5
    //std::string	data_mcphoparam6
    //std::string	data_mcphooffaxisvec;
    int	data_algorithmtype = 0;
    //std::string	data_crc
    MYSQL_TIME	data_updatetime;

    if (mysql_stmt_prepare(stmt, sSql.c_str(), (unsigned long)strlen(sSql.c_str())))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_prepare(), SELECT failed."<<mysql_stmt_error(stmt);
        return false;
    }

    //Fetch result set meta information
    prepare_meta_result = mysql_stmt_result_metadata(stmt);
    if (!prepare_meta_result) 
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_result_metadata(), returned no meta information "<<mysql_stmt_error(stmt);
        return false;
    }

    // Get total columns in the query and validate column count
    const unsigned int column_count= mysql_num_fields(prepare_meta_result);
    if (column_count != field_num)
    {
        TPS_LOG_DEV_ERROR<<"invalid column count returned by MySQL";
        return false;
    }

    MYSQL_BIND* bind = new MYSQL_BIND[column_count];
    memset(bind, 0, sizeof(MYSQL_BIND)*column_count);
    unsigned long* length = new unsigned long[column_count];
    memset(length, 0, sizeof(unsigned long) * column_count);
    my_bool* is_null = new my_bool[column_count];
    memset(is_null, 0, sizeof(my_bool) * column_count);
    //Execute the SELECT query
    char* data_string[field_num];
    for (int i=0; i<field_num; ++i)
    {
        const unsigned long ulSize = MAX_LENGTH[i] + 1;// + 1 for string's \0 
        data_string[i] = new char[ulSize];
        memset(data_string[i], 0, ulSize*sizeof(char));
    }
    if (mysql_stmt_execute(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_execute(), failed"<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    for (unsigned int i(0); i<column_count; ++i)
    {
        bind[i].is_null= &is_null[i];
        bind[i].length= &length[i];
        switch (i)
        {
        case 3: 	//mu float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mu;
            break;
        case 4: 	//mu_dx float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mu_dx;
            break;
        case 5: 	//mu_dr float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mu_dr;
            break;
        case 6: 	//mu_da float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mu_da;
            break;
        case 7: 	//mu_dv float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_mu_dv;
            break;
        case 8: 	//hvl_slope float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_hvl_slope;
            break;
        case 9: 	//density float
            bind[i].buffer_type= MYSQL_TYPE_FLOAT;
            bind[i].buffer= (char *)&data_density;
            break;
        case 10: 	//datatype int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_datatype;
            break;
        case 17: 	//mcphooffaxisvec blob
            bind[i].buffer_type= MYSQL_TYPE_BLOB;
            break;
        case 18: 	//algorithmtype int
            bind[i].buffer_type= MYSQL_TYPE_LONG;
            bind[i].buffer= (char *)&data_algorithmtype;
            break;
        case 20: 	//updatetime timestamp
            bind[i].buffer_type= MYSQL_TYPE_TIMESTAMP;
            bind[i].buffer= (char *)&data_updatetime;
            break;
        default:
            bind[i].buffer_type= MYSQL_TYPE_STRING;
            bind[i].buffer= (char *)data_string[i];
            bind[i].buffer_length= MAX_LENGTH[i];
            break;
        }
    }

    // Bind the result buffers
    if (mysql_stmt_bind_result(stmt, bind))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_bind_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    //Now buffer all results to client
    if (mysql_stmt_store_result(stmt))
    {
        TPS_LOG_DEV_ERROR<<" mysql_stmt_store_result() failed "<<mysql_stmt_error(stmt);
        for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
        DEL_ARRAY(bind);
        DEL_ARRAY(length);
        DEL_ARRAY(is_null);
        return false;
    }

    // Fetch all rows 
    int ret(1);
    RtAccessorychunk* pDis = nullptr;
    while(true)
    {
        ret = mysql_stmt_fetch(stmt);
        if (ret!=0 && ret!=MYSQL_DATA_TRUNCATED) break;
        pDis = new RtAccessorychunk();
        for (unsigned int i(0); i<column_count; ++i)
        {
            if (is_null[i]) continue;
            switch (i)
            {
            case 0: 	//uid varchar
                pDis->set_uid((char *)bind[i].buffer);
                break;
            case 1: 	//accessoryuid varchar
                pDis->set_accessoryuid((char *)bind[i].buffer);
                break;
            case 2: 	//commissionedunituid varchar
                pDis->set_commissionedunituid((char *)bind[i].buffer);
                break;
            case 3: 	//mu float
                pDis->set_mu(data_mu);
                break;
            case 4: 	//mu_dx float
                pDis->set_mu_dx(data_mu_dx);
                break;
            case 5: 	//mu_dr float
                pDis->set_mu_dr(data_mu_dr);
                break;
            case 6: 	//mu_da float
                pDis->set_mu_da(data_mu_da);
                break;
            case 7: 	//mu_dv float
                pDis->set_mu_dv(data_mu_dv);
                break;
            case 8: 	//hvl_slope float
                pDis->set_hvl_slope(data_hvl_slope);
                break;
            case 9: 	//density float
                pDis->set_density(data_density);
                break;
            case 10: 	//datatype int
                pDis->set_datatype(data_datatype);
                break;
            case 11: 	//mcphoparam1 varchar
                pDis->set_mcphoparam1((char *)bind[i].buffer);
                break;
            case 12: 	//mcphoparam2 varchar
                pDis->set_mcphoparam2((char *)bind[i].buffer);
                break;
            case 13: 	//mcphoparam3 varchar
                pDis->set_mcphoparam3((char *)bind[i].buffer);
                break;
            case 14: 	//mcphoparam4 varchar
                pDis->set_mcphoparam4((char *)bind[i].buffer);
                break;
            case 15: 	//mcphoparam5 varchar
                pDis->set_mcphoparam5((char *)bind[i].buffer);
                break;
            case 16: 	//mcphoparam6 varchar
                pDis->set_mcphoparam6((char *)bind[i].buffer);
                break;
            case 17: 	//mcphooffaxisvec blob
                {
                    const int total_length = length[i];
                    if (total_length < 1) break;
                    char *buf = new char[total_length + 1];
                    memset(buf, 0, sizeof(char) * total_length + 1);
                    bind[i].buffer = buf;
                    bind[i].buffer_length = total_length;
                    ret = mysql_stmt_fetch_column(stmt, &bind[i], i, 0);
                    if (0 == ret) pDis->set_mcphooffaxisvec(buf);
                    DEL_ARRAY(buf);
                }
                break;
            case 18: 	//algorithmtype int
                pDis->set_algorithmtype(data_algorithmtype);
                break;
            case 19: 	//crc varchar
                pDis->set_crc((char *)bind[i].buffer);
                break;
            case 20: 	//updatetime timestamp
                {
                    DATETIME_BOOST updatetime;
                    ConvertDateTime2Boost(data_updatetime, &updatetime);
                    pDis->set_updatetime(updatetime);
                }
                break;
            default:
                break;
            }
        }
        pDis->setdirty_all_fields(false);
        vList.push_back(pDis);
    }
    for (int i=0; i<field_num; ++i) DEL_ARRAY(data_string[i]);
    DEL_ARRAY(bind);
    DEL_ARRAY(length);
    DEL_ARRAY(is_null);

    // Free the prepared result metadata
    mysql_free_result(prepare_meta_result);

    // Close the statement
    if (mysql_stmt_close(stmt))
    {
        TPS_LOG_DEV_ERROR<<" failed while closing the statement " << mysql_stmt_error(stmt);
        return false;
    }

    return true;
}

RT_TPS_DATABASE_END_NAMESPACE